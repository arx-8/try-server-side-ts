version: "3"

services:
  app:
    build:
      # For Dockerfile COPY
      context: ../
      dockerfile: ./dockerfiles/app/Dockerfile
    env_file:
      - ./../.env
    networks:
      - app-nw
    ports:
      - 30001:30001

networks:
  app-nw:
